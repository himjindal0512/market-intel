services:
  - type: web
    name: market-intel
    runtime: python
    buildCommand: pip install -r requirements.txt && python volume_scan.py --backfill
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT
    plan: free
